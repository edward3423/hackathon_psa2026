{
  "name": "cascade-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright browser tests for the CASCADE demonstration (self-contained, not part of the npm workspace)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "~1.55.0",
    "@types/node": "^24.3.0",
    "typescript": "~5.9.2"
  }
}
